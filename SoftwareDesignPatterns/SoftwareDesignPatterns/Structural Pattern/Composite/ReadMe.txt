The Composite Pattern is used to treat individual objects and groups of objects uniformly.

It lets you build tree structures (e.g., folders and files) where leaf nodes and composite nodes are handled the 
same way.